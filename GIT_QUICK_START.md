# Git 快速配置指南

## 🎯 你的仓库信息

- **仓库名称**: tinygalarxyclash
- **仓库地址**: https://github.com/Tau3659/tinygalarxyclash.git
- **用户名**: Tau3659

## 🚀 快速开始（3步完成）

### 方法一：使用批处理脚本（最简单）

#### 1. 安装Git（如果还没安装）
- 访问：https://git-scm.com/download/win
- 下载并安装Git for Windows
- 安装时保持默认选项即可

#### 2. 运行配置脚本
双击运行 `setup_git.bat`

这个脚本会自动：
- ✅ 检查Git是否已安装
- ✅ 配置Git用户信息
- ✅ 初始化本地仓库
- ✅ 添加所有文件
- ✅ 提交到本地仓库
- ✅ 关联远程仓库

#### 3. 推送到GitHub
双击运行 `push_to_github.bat`

### 方法二：使用命令行

打开命令提示符（CMD）或PowerShell，在项目目录执行：

```bash
# 1. 初始化仓库
git init

# 2. 添加所有文件
git add .

# 3. 提交到本地
git commit -m "初始提交：无人机战略游戏 v1.0.0"

# 4. 设置主分支
git branch -M main

# 5. 关联远程仓库
git remote add origin https://github.com/Tau3659/tinygalarxyclash.git

# 6. 推送到GitHub
git push -u origin main
```

### 方法三：使用GitHub Desktop（推荐新手）

#### 1. 下载GitHub Desktop
- 访问：https://desktop.github.com/
- 下载并安装

#### 2. 登录GitHub账号
- 打开GitHub Desktop
- 点击 "Sign in to GitHub.com"
- 输入你的GitHub账号密码

#### 3. 添加本地仓库
- 点击 "File" -> "Add local repository"
- 选择你的项目文件夹
- 如果提示"未初始化"，点击"Create a repository"

#### 4. 发布到GitHub
- 点击 "Publish repository"
- 确认仓库名称为 `tinygalarxyclash`
- 点击 "Publish repository"

## 🔐 身份验证

### 如果推送时要求身份验证：

#### 选项1：使用Personal Access Token（推荐）

1. **生成Token**
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选 `repo` 权限
   - 生成并复制Token

2. **使用Token**
   - 推送时输入用户名：`Tau3659`
   - 密码处输入：刚才复制的Token

#### 选项2：使用SSH密钥

```bash
# 1. 生成SSH密钥
ssh-keygen -t ed25519 -C "你的邮箱@example.com"

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 3. 添加到GitHub
# 访问：https://github.com/settings/keys
# 点击 "New SSH key"，粘贴公钥

# 4. 修改远程地址
git remote set-url origin git@github.com:Tau3659/tinygalarxyclash.git
```

## 📝 日常使用

### 提交新的更改

```bash
# 1. 查看状态
git status

# 2. 添加修改的文件
git add .

# 3. 提交
git commit -m "描述你的修改"

# 4. 推送
git push origin main
```

### 拉取远程更新

```bash
git pull origin main
```

## ✅ 验证配置

推送成功后，访问你的仓库：
https://github.com/Tau3659/tinygalarxyclash

你应该能看到所有的游戏文件！

## 🐛 常见问题

### 1. "git不是内部或外部命令"
**解决**：需要先安装Git
- 下载：https://git-scm.com/download/win
- 安装后重启命令行

### 2. 推送被拒绝（403错误）
**解决**：需要配置身份验证
- 使用Personal Access Token
- 或使用GitHub Desktop

### 3. 推送被拒绝（non-fast-forward）
**解决**：先拉取远程更新
```bash
git pull origin main --rebase
git push origin main
```

### 4. 文件太大无法推送
**解决**：检查.gitignore，确保node_modules等大文件被忽略

## 📞 需要帮助？

如果遇到问题：
1. 查看错误信息
2. 参考 GITHUB_SETUP.md 详细文档
3. 或使用GitHub Desktop（最简单）

## 🎉 完成

配置完成后，你就可以：
- ✅ 版本控制你的代码
- ✅ 随时回退到之前的版本
- ✅ 与他人协作开发
- ✅ 在不同设备间同步代码

祝你使用愉快！🚀
